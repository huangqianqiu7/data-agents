# 统一配置参数 —— 设计文档 v3（2026-05-09）

> 状态：草案 v3（吸收 v2 审查 M1-M5 微调后的最终实施版）。
> 关联：
> - v1 设计：`./2026-05-09-统一配置参数-design.md`
> - v1 审查：`./2026-05-09-统一配置参数-design-审查结论.md`
> - v2 设计：`./2026-05-09-统一配置参数-design-v2.md`
> - v2 审查：`./2026-05-09-统一配置参数-design-v2-审查结论.md`（**Conditional Approve**）
> - 用户级实现计划：`C:\Users\18155\.windsurf\plans\unify-config-defaults-7b3fa6.md`
> - 上游：`../完善计划/2026-05-08-env-var-fallback-design.md`、`../完善计划/正式提交完善计划-v5.md` §二.4 / §二.8

## §0 v3 相对 v2 的变更摘要

按 v2 审查 §6 修订建议汇总，v3 做了以下精修。**v2 主要决策（D1
默认值保持 + LLM 身份三字段必填）一字未动**，仅微调测试数字 / 行号 /
用例骨架。

| 编号 | 严重度 | v2 现状 | v3 修订 |
| --- | --- | --- | --- |
| **M1** | Low | §5 / §7 风险 2 写"约 12 处 setenv" | 改为"**10 处**（每个 `def test_*` 一处，line 86 / 107 / 129 / 156 / 186 / 223 / 251 / 307 / 366 / 408）" |
| **M2** | Low | §9 Step 4 "预期 ~265 passed" | 改为"**预期约 268 passed**（基线 263 + 5 新增 - 0 删除）" |
| **M3** | Low | §5 改写行写 `test_*_model_name_falls_back_to_default*`（line 49 附近） | 改为精确名 `test_missing_model_name_falls_back_to_qwen`（line 48）；新名 `test_missing_model_name_raises_redacted_config_error`（与 line 17 `test_missing_model_api_url_raises_redacted_config_error` 同模式） |
| **M4** | Low | V5 / V6 setup 段未显式 `delenv("MODEL_API_KEY")` | 在 V5 / V6 setup 段补 `monkeypatch.delenv("MODEL_API_KEY", raising=False)`，避免外部 shell 注入真实 key 导致 CI flaky |
| **M5** | Medium | V7 缺 `_run_single_task_impl` monkeypatch | V7 完整测试骨架明确 `monkeypatch.setattr(submission, "_run_single_task_impl", _fake_impl)`，避免 600s 真实 LLM 构造拖慢 CI |
| 可选 1 | — | §9 Step 1 缺 baseline GREEN 校验 | 新增 Step 0：baseline `pytest tests/test_submission_*.py tests/test_phase5_config.py` 必须全 GREEN 才进入 RED 改写 |
| 可选 2 | — | §9 Step 6 缺 commit message 模板 | §9 末尾追加 commit message 范例（3 个 commit），方便 §7 风险 1 回滚锁定 sha |

## §1 背景与目标

### 1.1 用户诉求

> 让 `data_agent_langchain` 项目除了 `api_key` 和对应的 URL 以外的其他
> 参数，在本地运行和容器运行都是同一套，所有参数配置都集中在同一个
> 地方。

### 1.2 评测规则约束

`rules.zh.md` §5.2 / §5.4：LLM URL / Key **必须**通过环境变量注入，不得
硬编码到 YAML 或镜像中。v3（沿袭 v2）把 LLM **身份三字段**（URL /
Key / Name）一律提升为容器必填 env，把"非身份字段"全部下沉到
dataclass 默认。

### 1.3 目标

- `src/data_agent_langchain/config.py` 的 dataclass 默认值是**所有共享
  参数的唯一权威**。
- 本地（`load_app_config`）与容器（`build_submission_config`）对"非身
  份、非容器特化路径"的所有字段产出最终值**相等**。
- `submission.py` 不再包含可能与 dataclass 默认漂移的常量（`DEFAULT_*`
  三件套全删）。

## §2 现状盘点（事实复核已通过 v1 / v2 审查）

| 现状 | 位置 | 问题 |
| --- | --- | --- |
| `DEFAULT_MAX_WORKERS = 5` | `submission.py` 顶部 | 与 `RunConfig.max_workers = 5` 重复 |
| `DEFAULT_TASK_TIMEOUT_SECONDS = 600` | `submission.py` 顶部 | 与 `RunConfig.task_timeout_seconds = 600` 重复 |
| `DEFAULT_MODEL_NAME = "qwen3.5-35b-a3b"` | `submission.py` 顶部 | 与 `AgentConfig.model = "gpt-4.1-mini"` 不一致；让 LLM 身份在 env 缺失时静默 fallback |
| `_int_from_env("DABENCH_MAX_WORKERS", ...)` | `build_submission_config` | 让容器值有可能偏离本地默认 |
| `_int_from_env("DABENCH_TASK_TIMEOUT_SECONDS", ...)` | `build_submission_config` | 同上 |
| `MODEL_NAME` 缺失静默回退 `qwen3.5-35b-a3b` | `build_submission_config` 第 144 行 | 与 `MODEL_API_URL` 必填硬约束不对称 |
| 显式 `agent.action_mode="tool_calling"` | `build_submission_config` | dataclass 默认已为 `tool_calling`，重复 |
| 显式 `evaluation=EvaluationConfig(reproducible=False)` | `build_submission_config` | dataclass 默认已为 False，重复 |
| 显式 `tools=ToolsConfig()` | `build_submission_config` | 空构造，等价于不传 |
| 显式 `observability.langsmith_enabled=False` | `build_submission_config` | dataclass 默认已为 False；`run()` 内还有 `replace` 双保险 |

## §3 设计原则

1. **唯一权威**：`config.py` dataclass 默认是所有共享参数的唯一权威。
2. **最小覆写**：`build_submission_config` 仅显式覆写"容器特化的 3 条
   路径"（`/input`、`/tmp/dabench-runs`、`/app/gateway_caps.yaml`）。
3. **LLM 身份必经 env**（v2 强化）：`MODEL_API_URL` / `MODEL_API_KEY` /
   `MODEL_NAME` 三者全部为容器路径**必填**；缺失统一抛
   `SubmissionConfigError`，错误信息只点名 env 名，不含字面值。
   (`MODEL_API_KEY` 仍允许显式落到 `EMPTY_API_KEY` 哨兵值，与现有
   `test_missing_model_api_key_falls_back_to_empty` 行为对齐。)
4. **本地路径继续走 env 兜底**：`_overlay_agent_env_vars` 三字段（local
   path）的"YAML > env > dataclass 默认"协议**保持不变**——本地纯
   离线开发态仍可不设任何 env 启动。
5. **测试等价替换**：删除任何测试用例必须给出等价替换；不允许静默
   弱化覆盖。

## §4 变更项

### 4.1 `src/data_agent_langchain/submission.py`

**删除常量与导出**：

- 顶部常量：`DEFAULT_MAX_WORKERS`、`DEFAULT_TASK_TIMEOUT_SECONDS`、
  `DEFAULT_MODEL_NAME`。
- 辅助函数 `_int_from_env`。
- `__all__` 中对应导出项。

**修改 `build_submission_config()`**：

- `MODEL_API_URL` 缺失抛 `SubmissionConfigError`（保留）。
- `MODEL_API_KEY` 缺失回退 `EMPTY_API_KEY`（保留，因为 `EMPTY_API_KEY`
  是网关 LLM 显式哨兵值，不是泄漏风险）。
- **`MODEL_NAME` 缺失改抛 `SubmissionConfigError("Missing required
  environment variable: MODEL_NAME")`**（D1 新约束，沿袭 v2）。
- 不再读取 `DABENCH_MAX_WORKERS` / `DABENCH_TASK_TIMEOUT_SECONDS`。
- 删除显式 `action_mode="tool_calling"`、显式 `tools=ToolsConfig()`、
  显式 `evaluation=EvaluationConfig(reproducible=False)`、显式
  `observability.langsmith_enabled=False`：让 `AppConfig` 的
  `default_factory` 接管。
- 容器特化字段保留：`dataset.root_path=DEFAULT_INPUT_DIR`、
  `run.output_dir=DEFAULT_INTERNAL_RUNS_DIR`、
  `observability.gateway_caps_path=DEFAULT_GATEWAY_CAPS_PATH`。

**`run()` 不需要修改**（沿袭 v2 §4.1 F5 修订）：

当前 `submission.py:433-435`：

```python
max_workers=max_workers if max_workers is not None else config.run.max_workers,
task_timeout_seconds=0 if llm is not None else config.run.task_timeout_seconds,
```

已是条件覆盖。删除 `_int_from_env(...)` 后，`config.run.max_workers` 自动
落到 `RunConfig().max_workers`，无需改 `run()` 这一行。

**Docstring 清理**（沿袭 v2 §4.1 O3 / O4）：

- 模块 docstring 第 5 步（`submission.py:10`）：
  `MODEL_* / DABENCH_* 直接构造 AppConfig` → `从 MODEL_* env 构造
  AppConfig，其它字段走 dataclass 默认`。
- `build_submission_config` docstring "可选" 段（`submission.py:127-129`）：
  - 删除 `MODEL_NAME：缺失回退 qwen3.5-35b-a3b`。
  - 在"必填"段把 `MODEL_NAME` 与 `MODEL_API_URL` 并列登记。

### 4.2 `src/data_agent_langchain/config.py`

- `AgentConfig.model` 默认值**保持** `"gpt-4.1-mini"`（D1 沿袭 v2）。
- `_AGENT_ENV_VAR_MAP` 与 `_overlay_agent_env_vars` 不变（本地路径继续
  允许 env 缺失走 dataclass 默认）。
- `AgentConfig` docstring 追加一行说明：
  - 本地路径：`model` / `api_base` / `api_key` 受 `MODEL_*` env 兜底，
    YAML / env / dataclass 默认三层。
  - 容器路径：`MODEL_API_URL` / `MODEL_NAME` 均为必填 env，缺失抛
    `SubmissionConfigError`；`MODEL_API_KEY` 缺失回退 `EMPTY_API_KEY`。

### 4.3 文档同步（仅文字，不动代码逻辑）

| 文件 | 行号 | 修订 |
| --- | --- | --- |
| `src/data_agent_langchain/README.md` | §环境变量章节 | 移除 `DABENCH_*` 描述；明确 `MODEL_*` 三件套是仅有的 env 协议 |
| `src/data_agent_langchain/README.md` | line 233 | `max_workers=4` → `max_workers=5`（O5，stale 修正） |
| `Dockerfile` | line 36 | `所有配置走 MODEL_* / DABENCH_* 环境变量` → `所有配置走 MODEL_* 环境变量`（O2） |
| `src/计划/完善计划/2026-05-08-env-var-fallback-design.md` | 末尾 | 追加"2026-05-09 后续"小节：登记 `DABENCH_*` 已删除、容器路径 `MODEL_NAME` 升级为必填；行 45/74/115 关于 `gpt-4.1-mini` 的描述**仍正确**（D1 决策保持默认值）|
| `src/计划/完善计划/正式提交完善计划-v5.md` | §二.4 / §二.8 | `DEFAULT_MAX_WORKERS=5 / DEFAULT_TASK_TIMEOUT_SECONDS=600 / DEFAULT_MODEL_NAME="qwen3.5-35b-a3b"` 描述改写为"引用 dataclass 默认；`MODEL_NAME` 容器路径必填" |

## §5 测试影响（v3 修订表）

| 文件 | 修订类型 | 内容 |
| --- | --- | --- |
| `tests/test_submission_config.py` | **改写** | line 4 docstring：`DABENCH_* 环境变量` → `MODEL_* 环境变量直接构造 AppConfig` |
| `tests/test_submission_config.py` | **改写**（M3） | line 48 `def test_missing_model_name_falls_back_to_qwen(monkeypatch):` → 重命名为 `def test_missing_model_name_raises_redacted_config_error(monkeypatch):`，函数体改为 `pytest.raises(SubmissionConfigError)`，断言错误信息含 `"MODEL_NAME"`、不含字面值（与 line 17 `test_missing_model_api_url_raises_redacted_config_error` 完全同模式） |
| `tests/test_submission_config.py` | **新增** | `test_default_max_workers_matches_runconfig_default`：仅设 MODEL_API_URL + MODEL_NAME（**+ delenv MODEL_API_KEY**，M4），断言 `cfg.run.max_workers == RunConfig().max_workers` |
| `tests/test_submission_config.py` | **新增** | `test_default_task_timeout_matches_runconfig_default`：同上 setup，断言 `task_timeout_seconds == RunConfig().task_timeout_seconds` |
| `tests/test_submission_config.py` | **新增** | `test_submission_appconfig_equals_default_except_paths_and_identity`（D2 字段级等价断言；详见 §6 V6） |
| `tests/test_submission_runtime.py` | **删除** | line 338 `monkeypatch.setattr(submission, "DEFAULT_MAX_WORKERS", 1, raising=False)`（O1 dead code；同测试已有 `max_workers=1` 形参注入，line 344 覆盖） |
| `tests/test_submission_runtime.py` | **批量补 setenv**（M1 数字精确） | **共 10 处**（每个 `def test_*` 一处）`monkeypatch.setenv("MODEL_API_URL", ...)` 后同步 `monkeypatch.setenv("MODEL_NAME", "test-model")`，行号：86 / 107 / 129 / 156 / 186 / 223 / 251 / 307 / 366 / 408 |
| `tests/test_submission_runtime.py` | **新增** | `test_default_run_uses_runconfig_defaults_without_llm`：详见 §6 V7（M5 完整骨架） |
| `tests/test_submission_meta.py` | **新增** | `test_submission_all_no_default_constants`：断言 `DEFAULT_MAX_WORKERS / DEFAULT_TASK_TIMEOUT_SECONDS / DEFAULT_MODEL_NAME` 不在 `submission.__all__` 中（防回归） |
| `tests/test_phase5_config.py` | **不改** | line 196 `assert cfg.agent.model == "gpt-4.1-mini"` 仍正确（D1 决策保持默认值） |

**严禁弱化**：所有删除项必须有等价替换断言；批量补 setenv 不得借机删
原有断言。

## §6 验证清单（v3 增补 M4 / M5 修订）

- [ ] **V1**：`git status` 在 worktree 中只显示本次预期文件改动。
- [ ] **V2**：`pytest` 全绿（基线 263 → v3 后**预期约 268 passed**，新增 5
      个用例：V5×2 + V6 + V7 + meta 防回归；删除 0；改写 1 不计入数量）。
- [ ] **V3**：`git grep DABENCH_ -- src/ tests/ Dockerfile docker/ README.md` 无匹配。
- [ ] **V4**：`git grep -E "DEFAULT_MAX_WORKERS|DEFAULT_TASK_TIMEOUT_SECONDS|DEFAULT_MODEL_NAME" -- src/data_agent_langchain` 无匹配。
- [ ] **V5**：仅设 `MODEL_API_URL` + `MODEL_NAME`、显式 `delenv("MODEL_API_KEY")` 时（M4）：`build_submission_config()` 不报错，且
      - `cfg.run.max_workers == RunConfig().max_workers`
      - `cfg.run.task_timeout_seconds == RunConfig().task_timeout_seconds`
      - `cfg.agent.action_mode == AgentConfig().action_mode`
      - `cfg.agent.api_key == "EMPTY"`。
- [ ] **V6**（D2 字段级等价；M4 加 delenv）：

  ```python
  from dataclasses import replace
  from data_agent_langchain import submission
  from data_agent_langchain.config import default_app_config

  def test_submission_appconfig_equals_default_except_paths_and_identity(monkeypatch):
      """V6：build_submission_config 与 default_app_config 在非身份/非容器
      路径字段上 dataclass 级别相等。未来若有人偷偷加新字段差异立即 RED。"""
      monkeypatch.setenv("MODEL_API_URL", "http://internal.model/v1")
      monkeypatch.setenv("MODEL_NAME", "test-model")
      monkeypatch.delenv("MODEL_API_KEY", raising=False)  # M4

      cfg = submission.build_submission_config()
      baseline = default_app_config()
      normalized = replace(
          cfg,
          dataset=baseline.dataset,
          agent=replace(cfg.agent, model=baseline.agent.model,
                        api_base=baseline.agent.api_base,
                        api_key=baseline.agent.api_key),
          run=replace(cfg.run, output_dir=baseline.run.output_dir),
          observability=replace(
              cfg.observability,
              gateway_caps_path=baseline.observability.gateway_caps_path,
          ),
      )
      assert normalized == baseline
  ```

- [ ] **V7**（D3 测试旁路守护；M5 补 monkeypatch 骨架）：

  ```python
  def test_default_run_uses_runconfig_defaults_without_llm(tmp_path, monkeypatch):
      """V7：不传 llm 时 summary['task_timeout_seconds'] 走 RunConfig 默认；
      避免 600s 真实 LLM 构造拖慢 CI（M5 关键 monkeypatch）。"""
      from data_agent_langchain import submission
      from data_agent_langchain.config import RunConfig
      from data_agent_langchain.run.runner import TaskRunArtifacts

      monkeypatch.setenv("MODEL_API_URL", "http://internal.model/v1")
      monkeypatch.setenv("MODEL_NAME", "test-model")
      monkeypatch.delenv("MODEL_API_KEY", raising=False)

      def _fake_impl(*, task_id, config, run_output_dir, llm, graph_mode):
          # 同 test_per_task_failure_does_not_block_other_tasks 模板
          return TaskRunArtifacts(
              task_id=task_id, succeeded=True,
              prediction_csv_path=None, run_dir=run_output_dir / task_id,
          )

      monkeypatch.setattr(submission, "_run_single_task_impl", _fake_impl)
      dirs = _submission_dirs(tmp_path)  # 已有 helper
      dirs["input_dir"].mkdir()
      _make_task(dirs["input_dir"], "task_1")
      _make_caps(dirs["gateway_caps_path"])

      summary = submission.run(
          **dirs,
          register_signals=False,
          llm=None,  # 关键：不传 llm 走 RunConfig 默认 timeout
          action_mode_override="json_action",
      )

      assert summary["task_timeout_seconds"] == RunConfig().task_timeout_seconds
      assert summary["max_workers"] == RunConfig().max_workers
  ```

  **对偶分支**（`llm is not None` 时 `task_timeout_seconds == 0`）已被
  现有 `test_caps_file_with_tool_calling_true_proceeds` 等多条测试隐式
  覆盖；无需重复写显式断言。

## §7 风险与回滚

1. **风险**：已有镜像 / 提交脚本依赖 `DABENCH_*` env，或调用方未设
   `MODEL_NAME`。
   **缓解**：评测系统的 starter kit 文档明确提示 `MODEL_*` 三件套必填；
   `DABENCH_*` 是项目内部调优开关，未公开宣传。
   **回滚剧本**：
   - 单文件回滚：`git checkout <baseline_sha>^ -- src/data_agent_langchain/submission.py
     src/data_agent_langchain/config.py`，再补回测试改动；
   - 整体回滚：`git revert <feature_commit_range>`（按 §9 Step 6 拆 3 个
     commit 的 sha 锁定范围）。
2. **风险**：D1 `MODEL_NAME` 必填硬约束在 `tests/test_submission_runtime.py`
   引发 **10 处**（M1 精确）`setenv` 增补，量大易漏。
   **缓解**：用 conftest 级 fixture（如已有）统一 setup；TDD §9 Step 1
   先改测试 → 一次性看到所有 RED 失败定位漏改。
3. **风险**：删除测试用例被误判为弱化测试。
   **缓解**：每条删除用例必须有等价替换（见 §5）；PR 描述里逐条对账。

## §8 不在范围（沿袭 v2 §8）

- 不动 `Dockerfile` 的 build / ENTRYPOINT / dependency 安装层；仅修订
  line 36 注释（O2 例外登记）。
- 不动 `docker/gateway_caps.yaml` 与 `.dockerignore`：本次仅 Python 端
  配置收敛。
- 不动 `configs/local.yaml`：用户本地若已写过 `agent.max_steps` 等字段，
  行为不变（YAML 显式值仍然赢）。
- 不动 `data_agent_langchain/llm/factory.py`：它已直接消费
  `cfg.agent.api_base/api_key/model`，上游统一即可。
- **不修 `README.md` line 232 `AgentConfig` 表格**（O6 既存 stale）：
  `max_steps=20 / max_replans=2 / max_gate_retries=2` 与实际 60/4/6 不
  符是历史 doc bug，与本次 LLM 身份 / 容器参数收敛无直接耦合；留给
  独立的 README 同步任务处理。本次仅修 line 233 `RunConfig.max_workers`
  这一项与本次主题直接相关的 stale。
- 不修主 worktree 当前的工作树状态。

## §9 TDD 执行顺序（v3 完整版）

### Step 0：baseline GREEN 校验（v3 新增可选 1）

```powershell
pytest tests/test_submission_config.py tests/test_submission_runtime.py `
       tests/test_submission_meta.py tests/test_phase5_config.py
```

**预期**：基线 32 个 test 全 GREEN（8 + 10 + 4 + 10 = 32）。任何 fail 都
说明工作树状态有别的污染，必须先解决再开始 RED 改写。

### Step 1：RED —— 改测试

按 §5 表格修改 4 个测试文件：

- `test_submission_config.py`：line 4 docstring 清理 + line 48 函数改写
  + 新增 V5 / V5' / V6（共 +3 个新 test 函数）。
- `test_submission_runtime.py`：删 line 338 dead code + 10 处补 MODEL_NAME
  setenv + 新增 V7（+1 个新 test 函数）。
- `test_submission_meta.py`：新增 `__all__` 防回归断言（+1 个新 test 函数）。

跑 Step 0 同样 4 文件 pytest，**预期 RED**（多条 fail：line 48 改名后旧名找
不到、新断言 fail，因为 `submission.py` 还没改）。

### Step 2：GREEN —— 改代码

- `submission.py`：删 3 个 `DEFAULT_*` 常量与 `_int_from_env`、`__all__` 同步删；
- `submission.py`：`build_submission_config()` 按 §4.1 重写（含 MODEL_NAME 必填抛错）；
- `submission.py`：模块 docstring + `build_submission_config` docstring 改写；
- `config.py`：`AgentConfig` docstring 追加 env 协议说明。

跑同 4 文件 pytest，**预期 GREEN**。

### Step 3：REFACTOR / 文档同步

按 §4.3 表逐行修订：README、Dockerfile:36、env-var-fallback-design.md
末尾"后续"小节、v5 §二.4 / §二.8。

### Step 4：全量回归

```powershell
pytest
```

**预期约 268 passed**（M2：基线 263 + 5 新增 - 0 删除）。

### Step 5：卫生检查

执行 §6 V3 / V4 grep 命令，预期 0 命中。

### Step 6：拆 commit

按 "测试 → 代码 → 文档" 顺序拆 3 个 commit（v3 沿袭 v2 决策；v2 审查
§4.2 已确认这与 TDD 原则一致）。

**Commit message 范例**（v3 新增可选 2）：

```
test: tighten config tests for unified defaults (D1 + V5-V7)

- rename test_missing_model_name_falls_back_to_qwen → _raises_redacted_config_error
- add MODEL_NAME setenv to 10 runtime tests; drop DEFAULT_MAX_WORKERS dead code
- add field-level equivalence test (V6) and timeout-bypass guard (V7)
- add __all__ no-default-constants regression in test_submission_meta

Refs: src/计划/统一配置/2026-05-09-统一配置参数-design-v3.md §5
```

```
feat: collapse submission defaults into config.py dataclasses

- remove DEFAULT_MAX_WORKERS / DEFAULT_TASK_TIMEOUT_SECONDS / DEFAULT_MODEL_NAME
- remove _int_from_env and DABENCH_* env reads
- elevate MODEL_NAME to required env in build_submission_config (D1)
- AgentConfig.model default kept as "gpt-4.1-mini" (D1 v2 review)

Refs: src/计划/统一配置/2026-05-09-统一配置参数-design-v3.md §4
```

```
docs: sync README/Dockerfile/v5 plan with unified config defaults

- README §env-vars: drop DABENCH_*; max_workers=4 → 5 (O5)
- Dockerfile:36 comment: drop "/ DABENCH_*" (O2)
- env-var-fallback-design.md: append 2026-05-09 follow-up note
- 正式提交完善计划-v5.md §二.4 / §二.8: cite dataclass defaults

Refs: src/计划/统一配置/2026-05-09-统一配置参数-design-v3.md §4.3
```

### Step 7（可选）：grep stale 自检

```powershell
git grep -E "qwen3.5-35b-a3b|DABENCH_|DEFAULT_MAX_WORKERS|DEFAULT_TASK_TIMEOUT_SECONDS|DEFAULT_MODEL_NAME" -- src/ tests/ Dockerfile docker/ README.md
```

**预期**：仅在 `src/计划/` 内部（设计文档自我引用）出现，不在 src/ 与
tests/ 主代码出现。

## §10 安全 / 评测约束（沿袭 v1 / v2，复核全部通过）

| 约束 | v3 状态 |
| --- | --- |
| `rules.zh.md` §5.2 / §5.4：LLM URL/Key 必经 env | ✓ v3（沿袭 v2）把 MODEL_NAME 升级为必填 env，**加强**该约束 |
| 错误信息脱敏 | ✓ §6 V5 / §5 改写明确"只点名 env 名，不含字面值" |
| 不读 / 不改 `configs/react_baseline.example.yaml` | ✓ 不触达 |
| `submission.py` 不读 YAML | ✓ §4.1 仅删常量与 `_int_from_env` |
| `langsmith_enabled = False` 不放松 | ✓ ObservabilityConfig 默认 False；`run()` `replace` 双保险保留 |
| `evaluation.reproducible = False` 不放松 | ✓ EvaluationConfig 默认 False，删除显式覆写等价 |
| `default_app_config()` 不引入新依赖 | ✓ V6 仅复用 `dataclasses.replace` 与 `default_app_config`，已存在 |
| 测试文件不含真实 key / URL | ✓ 全部用 `http://internal.model/v1` / `test-model` 占位 |
